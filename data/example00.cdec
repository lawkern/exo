import format;

function format_print(message string) void
{
   return(0);
}

function main(argument_count int) int
{
   format_print("hello");

   var index int = 0;
   for(1)
   {
      var x int = 80;
      var y int = 100;
   }

   return(2);
}
